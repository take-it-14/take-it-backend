spring:
    datasource:
      driver-class-name: org.postgresql.Driver
      username: takeit_review
      password: takeit_review
      url: jdbc:postgresql://localhost:15432/takeit_review
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          highlight_sql: true
          default_batch_fetch_size: 10
      open-in-view: false
    data:
      redis:
        host: localhost
        port: 6379
        password: systempass

server:
  port: 19096

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/

cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET}
    credentials:
      access-key: ${S3_ACCESS_KEY}
      secret-key: ${S3_SECRET_KEY}
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: debug
    com.takeit: debug
    org.springframework: warn

